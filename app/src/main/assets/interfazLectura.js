//var InterfazLectura= (function(){

        function lecturaArchivo(archivo,callback){

            Android.lecturaArchivo(archivo);
        }


        function onDevolucion(){



        }
/*
           function onRespuesta(url, exito){
                console.log("respuesta para "+url);
                if (callbacksRegistradas[url]) {
                    var callback = callbacksRegistradas[url];
                    callback(exito);
                } else {
                    console.error("El callback no existe para la url "+url);
                }
             }
             */

//})();